<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.gen69">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GEN69"
        tools:targetApi="31">

        <!-- ✅ Splash screen as launcher -->
        <activity
            android:name=".Splashscreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auth -->
        <activity android:name=".MainActivity" android:exported="true" />
        <activity android:name=".SignupActivity" android:exported="true" />

        <!-- Home -->
        <activity android:name=".HomeActivity" android:exported="true" />


        <!-- Plots & PGHostels -->
        <activity android:name=".SellerActivity" android:exported="true" />
        <activity android:name=".BuyerActivity" android:exported="true" />
        <activity android:name=".DashboardActivity" android:exported="true"/>


        <!-- Dashboards -->

        <!-- Profile -->
        <activity android:name=".ProfileActivity" android:exported="true"/>



        <!-- Wishlist & Cart -->
        <activity android:name=".WishlistActivity" android:exported="true" />
        <activity android:name=".CartActivity" android:exported="true" />

        <!-- Common -->



    </application>

</manifest>
